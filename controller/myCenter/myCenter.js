const {exec, escape} = require('../../db/mysql')
const {getPassword} = require('../../utils/cryp')
// ------------------app接口------------------------

// 获取我的日记
// 获取我的关注

// 获取钱包
// 获取个人信息

module.exports = {
  
}